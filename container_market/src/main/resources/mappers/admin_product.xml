<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kb1.containerMarket.repository.admin.ProductManagementRepository">
    <resultMap id="product_category" type="com.kb1.containerMarket.web.domain.ProductCategory">
        <result property="category_id" column="id"/>
        <result property="group_id" column="group_id"/>
        <result property="category_name" column="category_name"/>
    </resultMap>


    <select id="getCategoryList" resultMap="product_category">
        select
            id,
            category_name
        from
            pdt_category_mst
    </select>

    <insert id="saveProductMst" parameterType="com.kb1.containerMarket.web.domain.Product">
        insert into
        product_mst
        values(
        0,
        #{pdt_name},
        #{category_id},
        #{pdt_price},
        #{pdt_simple_info},
        #{pdt_detail_info},
        now(),
        now()
        )
    </insert>

    <select id="getProducts" parameterType="int" resultType="com.kb1.containerMarket.web.domain.admin.AdminProduct">
        select
            pm.id,
            pm.pdt_name,
            pm.pdt_price,
            pcm.category_name,
            pm_cnt.product_total_count
        from
            pdt_mst pm
            left outer join pdt_category_mst pcm on(pcm.id = pm.category_id)
            left outer join (   select count(*) product_total_count
                                from pdt_mst
                                 ) pm_cnt on (1=1)
        limit #{page}, 10
    </select>
</mapper>